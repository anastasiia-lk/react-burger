import {createContext} from 'react';

export const APIContext = createContext(null);
export const AddsIngredientsContext =  createContext(null);
export const BunsIngredientsContext = createContext(null);